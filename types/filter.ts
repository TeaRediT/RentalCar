export const defaultFilter = {
  brand: "",
  rentalPrice: "",
  minMileage: "",
  maxMileage: "",
};

export interface activeFilters {
  brand: string;
  rentalPrice: string;
  minMileage: string;
  maxMileage: string;
}
